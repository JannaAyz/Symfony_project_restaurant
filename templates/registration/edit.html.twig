<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <title>confirmation de suppression</title>
    <link href="https://fonts.googleapis.com/css2?family=Montserrat:ital,wght@0,300;0,400;0,500;1,500&display=swap" rel="stylesheet">
    <style>
      body{
        font-family: 'Montserrat', sans-serif;
        margin-top: 10%;
        overflow: hidden;
      }
      .outline ul li{
        color:red;
        list-style:none;
        margin-left:-30px;
        margin-top:10px;
      }
      h3{
        margin-top: 10%;
      }
      h5 {
        font-weight: 400;
        transform: translateY(-80%);
        color: #95C623;
        animation: visible 200ms ease-out forwards;
      }
      #note {
        font-size: 10px;
        width: 80%;
        margin-bottom: 15px;
      }
      body form div ul  li{
        list-style-type: none;
        margin-left: -40px;
        color: #DD2D4A;
        font-weight: 10px;
      }
      {# .text-danger{
        list-decoration: none;
      } #}
      .form-group{
        font-size:12px; 
        margin-top: 10px;
      }
      label {
        transform: translateY(-50px);
      }
      .input{
        font-size: 10px;
        padding: 5px;
        border-radius: 0;
        border: 2px solid #95C623;
      }
      #edit_registration_form_password_second {
        transform:translateX(19px);
      }
      button{
        margin: 30px 0 0;
        color: black;
        text-transform: uppercase;
        border: 2px solid black;
        background: rgba(149, 198, 35, .8);
        backdrop-filter: blur(10px);
        padding: .5rem 4rem;
        font-size: 14px;
        cursor: pointer;
        position: absolute;
        bottom: 0%;
        left:18%;
      }
      button:hover {
        background: rgba(232, 233, 243, .5);
        border: 2px solid #95C623;
        text-shadow: 2px 1px 1px #95C623;
      }
      input[type="file" i], input[type="file" i] button {
        color: grey;
        font-size: 10px;
        cursor: pointer;
      }

      .form-group, h3, #note {
        
      }
      @keyframes visible {
        from {
            opacity:0;
        } 
        to {
            opacity:1;
        }
      }
  </style>
</head>
<body>
    <h3>Modifier les informations</h3>
    <p id="note">Pour des raisons de sécurité, seule la photo de profil est modifiable.</p>
      {{form_start(registrationForm)}}
        <div class="form-group">
          {{ form_label(registrationForm.password, null) }}
          {{ form_widget(registrationForm.password, {'attr': {'class' : 'input', 'autocomplete' :"jesuisadmin"}}) }}
          {{ form_errors(registrationForm.password, {'attr': {'class' : 'text-danger'}}) }}
        </div>
        {# <div class="form-group">
          {{ form_label(registrationForm.password.second, null) }}
          {{ form_widget(registrationForm.password.second, {'attr': {'class' : 'input'}}) }}
          {{ form_errors(registrationForm.password.second, {'attr': {'class' : 'text-danger'}}) }}
        </div> #}
        <div class="form-group">
          {{ form_label(registrationForm.imageUrl, null) }}
          {{ form_widget(registrationForm.imageUrl) }}
          {{ form_errors(registrationForm.imageUrl, {'attr': {'class' : 'text-danger'}}) }}
        </div>

      {{ form_end(registrationForm, {'label': 'Modifier', 'attr': {'class': 'btn btn-primary'}}) }}
      {% if formSubmittedAndValid %}
          <h5>La photo de profil a été modifiée</h5>
      {% endif %}
</body>
</html>

